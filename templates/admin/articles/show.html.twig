{% extends 'admin/accueil.html.twig' %}

{% block title %}Admin - Prophac{% endblock %}

{% block PageTitle %} Détail de l'article {{ article.titre }}{% endblock %}

{% block body %}
    {# {{ dump(article) }} #}
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ article.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ article.titre }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ article.contenu }}</td>
            </tr>
            <tr>
                <th>CreatedDate</th>
                <td>{{ article.createdDate ? article.createdDate|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>
    <div class="d-flex">
        <a class="btn btn-success text-light ms-2 mt-2" href="{{ path('app_articles_edit', {'id': article.id}) }}">Modifier l'article</a>
        {{ include('admin/articles/_delete_form.html.twig') }}
    </div>
        <a class="btn btn-prophac ms-2 mt-2" href="{{ path('app_articles_index') }}">Retourner sur la liste des articles</a>
{% endblock %}
