{% extends 'admin/accueil.html.twig' %}

{% block title %}Admin - Prophac{% endblock %}

{% block PageTitle %} Détail du département {{ departement.nom }}{% endblock %}

{% block body %}
{# {{ dump(departement) }} #}
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ departement.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ departement.nom }}</td>
            </tr>
            <tr>
                <th>Logo</th>
                <td><img src="../uploads/{{ departement.logo}}" alt="picto-{{ departement.nom }}" class="img-fluid" width="15%"></td>
            </tr>
            {# <tr>
                <th>Collaborateur</th>
                <td>
                    {% for collaborateur in departement.collaborateur %}
                        {{ collaborateur}} <br>
                    {% endfor %}
                </td>
            </tr> #}
        </tbody>
    </table>
    <div class="d-flex">
        <a class="btn btn-success text-light ms-2 mt-2" href="{{ path('app_departements_edit', {'id': departement.id}) }}">Modifier ce département</a>
        {{ include('departements/_delete_form.html.twig') }}
    </div>
    <a class="btn btn-prophac ms-2 mt-2" href="{{ path('app_departements_index') }}">Retourner sur la liste des départements</a>
{% endblock %}
