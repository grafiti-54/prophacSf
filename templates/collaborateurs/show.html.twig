{% extends 'base.html.twig' %}

{% block title %}Collaborateurs{% endblock %}

{% block body %}
    <h1>Collaborateurs</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ collaborateur.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ collaborateur.email }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ collaborateur.roles ? collaborateur.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ collaborateur.password }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ collaborateur.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ collaborateur.prenom }}</td>
            </tr>
            <tr>
                <th>Photo</th>
                <td>{{ collaborateur.photo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_collaborateurs_index') }}">back to list</a>

    <a href="{{ path('app_collaborateurs_edit', {'id': collaborateur.id}) }}">edit</a>

    {{ include('collaborateurs/_delete_form.html.twig') }}
{% endblock %}
