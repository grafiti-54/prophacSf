<form method="post" action="{{ path('app_collaborateurs_delete', {'id': collaborateur.id}) }}" onsubmit="return confirm('Voulez vous vraiment supprimer ce collaborateur?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ collaborateur.id) }}">
    <button class="btn btn-danger text-light ms-1 mt-2">Supprimer ce collaborateur</button>
</form>
